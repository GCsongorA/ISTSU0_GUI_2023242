<UserControl x:Class="ISTSU0_GUI_2023242.Client.Views.Guitar.OneGuitarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ISTSU0_GUI_2023242.Client.Views.Guitar"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="DarkGray">
    <Grid>
        <StackPanel>
            <Label Content="Guitar ID:" Margin="5" Padding="10"/>
            <TextBox x:Name="txbGuitarGuid" Margin="5" Padding="10"/>
            <Button Content="Run" Click="Button_Click" Margin="5" Padding="10"/>
            <DataGrid x:Name="DataGrid" Background="DarkGray">
                <DataGrid.Resources>
                    <Style TargetType="{x:Type DataGridColumnHeader}">
                        <Setter Property="Background" Value="Cyan" />
                        <Setter Property="BorderThickness" Value="0,0,1,2" />
                        <Setter Property="BorderBrush" Value="Black" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                    </Style>
                </DataGrid.Resources>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
