<UserControl x:Class="ISTSU0_GUI_2023242.Client.Views.Guitar.CreateGuitarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ISTSU0_GUI_2023242.Client.Views.Guitar"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <StackPanel>
            <Label Content="Brand:" />
            <TextBox x:Name="txbBrand" />
            <Label Content="Model:" />
            <TextBox x:Name="txbModel" />
            <Label Content="Body type (FlyingV, LesPaul, Stratocaster, SuperStrat, Telecaster):" />
            <TextBox x:Name="txbBodyType" />
            <Label Content="Guitarist (GUID):" />
            <TextBox x:Name="txbGuitarist" />
            <Button Content="Add" Click="Add_Click" />
        </StackPanel>
    </Grid>
</UserControl>
